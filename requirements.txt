# =============================================================================
# Dehesa Tree Crown Segmentation (YOLOv11) — Requirements
# Notebook: Crown_detector.ipynb
# =============================================================================
# Python: >= 3.10  (tested on 3.11.14)
#
# Install:
#   pip install -r requirements.txt
#
# PyTorch must be installed separately to match your CUDA version:
#   # CUDA 12.6
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#   # CPU only
#   pip install torch torchvision
# =============================================================================

# --- Core Scientific Stack ---
numpy>=1.24.0
pandas>=2.0.0

# --- Deep Learning / Detection ---
# torch / torchvision: install separately (see header)
ultralytics>=8.4.34             # YOLOv11n-seg — crown instance segmentation

# --- Geospatial & GIS ---
rasterio>=1.3.0                 # Orthophoto I/O, windowed tiling, affine transforms
geopandas>=0.14.0               # GeoDataFrame, spatial-join NMS, GeoPackage export
shapely>=2.0.0                  # Crown polygon geometry, mask-to-polygon

# --- Notebook Support ---
ipykernel
jupyterlab>=4.0.0
