# Raster / vector data
*.tif
*.tiff
*.gpkg
*.zip

# Model weights. best.pt is the exception: without it the repo is not runnable,
# so it is tracked deliberately. Any other checkpoint stays out.
*.pt
!models/best.pt
*.onnx

# Training dataset (available on Roboflow â€” see README)
Detector_Copas_yaml/

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
